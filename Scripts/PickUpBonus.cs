using UnityEngine;
using System.Collections;

// The PickUpBonus script contains values that corresspond with values in the Inventory.
// Once an object with this script is picked up the bonuses are added.
public class PickUpBonus : MonoBehaviour 
{
	public int ammo = 0;
	public int health = 0;
	public int slomo = 0;
	public int grenades = 0;
}
